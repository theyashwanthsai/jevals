[project]
name = "jevals"
version = "0.1.0"
description = "LLM/LLM agent eval framework. Research preview."
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["jevals"]

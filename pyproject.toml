[project]
name = "jevals"
version = "0.1.0"
description = "Evals for AI systems, graded by a calibrated decision model"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["jevals"]
